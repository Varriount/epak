Run "make" to build the Objective-C library.
Run "make install" to install the C/Python library/module.
See test.c and test.py inside example for how stuff works.
API reference generated in the docs directory with Doxygen.
